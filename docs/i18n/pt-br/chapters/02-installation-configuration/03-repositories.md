# 2.3 Repositórios locais e remotos

Comece localmente com `git init` ou copie um projeto existente com `git clone <repository-url>`. Inspecione o remoto configurado com:

```bash
git remote -v
```

Use um repositório de prática enquanto estiver aprendendo. Nunca experimente comandos destrutivos em um checkout de produção.
