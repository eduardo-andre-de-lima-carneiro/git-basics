# 2.1 Install Git

Git 2.23 or newer is recommended for the `git switch` and `git restore` examples used later. Older versions may require equivalent commands such as `git checkout`.

Install Git using the package manager or installer recommended for your operating system. Confirm the installation:

```bash
git --version
```

The exact version may vary. The command should print a version instead of an error.
