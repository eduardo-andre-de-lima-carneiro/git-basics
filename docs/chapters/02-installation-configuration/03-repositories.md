# 2.3 Local and Remote Repositories

Start locally with `git init`, or copy an existing project with `git clone <repository-url>`. Inspect the configured remote with:

```bash
git remote -v
```

Use a practice repository while learning. Never experiment with destructive commands in a production checkout.
