# 3.2 Commits and History

Useful inspection commands include:

```bash
git log --oneline --decorate --graph
git show <commit>
git diff <commit-a> <commit-b>
```

Prefer focused commits that represent one logical change. History is easier to review when commit messages use an imperative, specific description.
